import 'package:flutter/material.dart';

class RoutinesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Routines', style: TextStyle(fontSize: 24)),
    );
  }
}
